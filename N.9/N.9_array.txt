#pragma warning(disable : 4996)
#include<stdio.h>
#include<conio.h>// getch();

void main
{
// array
	/*
	// 1.
	int a[3];
	a[0] = 10;
	a[1] = 20;
	a[2] = 30;

	// 1-1
		int a, b, c;

		a = 10;
		b = 20;
		c = 30;

	// 2.
	int a[3] = { 10, 20, 30 };

	// 2-1
	int a = 10, b = 20, b = 30;

	// 3.
	int i, a[3];

	for (i = 0; i < 3; i++)
	{
		printf("정수 입력 : ");
		scanf("%d", &a[i]);
	}
	printf("받은 정수 : ");
	for (i = 0 ; i < 3; i++) printf("%3d", a[i]);

	// 3-1
	int a, b, c;

	printf("첫번째 정수 : ");
	scanf("%d", &a);
	printf("두번째 정수 : ");
	scanf("%d", &b);
	printf("세번째 정수 : ");
	scanf("%d", &c);
	*/
}