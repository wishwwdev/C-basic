#pragma warning(disable : 4996)
#include<stdio.h>
#include<conio.h>// getch();

void main
{
// Selection Sort
	// 내림차순
	/*
	int arr[5];

	for (int i = 0; i < 4; i++)
		for (int j = i + 1; j < 5; j++)
			if (arr[i] < arr[j])
			{
				int temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
	*/
	

	// 오름차순
	/*
	int arr[5];

	for (int i = 0; i < 4; i++)
		for (int j = i + 1; j < 5; j++)
			if (arr[i] > arr[j])
			{
				int temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
	*/
}